# UI TODOS

Complete items in order. Remove an item only after finishing it.

1. Scaffold `crates/app/ui` with `npx sv@0.17 create` (SvelteKit minimal, TypeScript, Prettier, ESLint, Vitest, Playwright, Tailwind). Pin `svelte` 5.57, `@sveltejs/kit` 2.70, `vite` 8.3, `tailwindcss` 4.3 with `@tailwindcss/vite`, `typescript` 6.0, `@sveltejs/adapter-static` 3.0, `@skeletonlabs/skeleton` 5.0, `@skeletonlabs/skeleton-svelte` 5.0, `@lucide/svelte` 1.47, `@fontsource-variable/inter` 5.3, `@fontsource-variable/jetbrains-mono` 5.3. Scripts `dev`, `build`, `check`, `lint`, `format`, `test`, `test:e2e`. Ignore `node_modules`, `.svelte-kit`, `build`, `test-results`.
2. `svelte.config.js`: adapter-static with `pages` and `assets` set to `process.env.DISCOCLIP_UI_OUT ?? 'build'` and `fallback: 'index.html'`. Root `+layout.ts` exports `ssr = false` and `prerender = false`. `vite.config.ts`: `tailwindcss()` before `sveltekit()`, `server.proxy['/api']` to `process.env.DISCOCLIP_API ?? 'http://127.0.0.1:8080'` with `changeOrigin: false` so the backend's Origin check passes and event streams flow unbuffered. `npm run build` with `DISCOCLIP_UI_OUT` set writes `index.html` at the root of that directory.
3. `src/app.html`: `<html lang="en" data-theme="discoclip">`, a literal `<head>` tag on its own line (the server inserts media page metadata after it), viewport meta, `favicon.svg`, `favicon.ico`, `apple-touch-icon.png`, `site.webmanifest`, one `theme-color` meta per mode, then one inline script that reads `localStorage['discoclip.mode']` and `prefers-color-scheme` and sets `data-mode` on `<html>` before paint. The script is static text so `build.rs` hashes it into the CSP.
4. `src/app.css`: `@import 'tailwindcss'`, `@import '@skeletonlabs/skeleton'`, `@import '@skeletonlabs/skeleton-svelte'`, `@import './lib/brand/theme.css'`, `@custom-variant dark (&:where([data-mode=dark], [data-mode=dark] *))`, the Inter Variable and JetBrains Mono Variable fontsource imports (fonts ship in the bundle because the CSP allows `font-src 'self'` only), `color-scheme` following `data-mode`, root background and text from the theme tokens.
5. Palette. `src/lib/brand/theme.css` is a Skeleton theme named `discoclip` generated from these anchors: primary `#f2542d` coral, secondary `#2ec4b6` teal, tertiary `#f5b700` amber, success `#22c55e`, warning `#f59e0b`, error `#ef4444`, surface a cool grey from `#f7f7f8` (50) to `#0b0b0e` (950). Base and heading font Inter Variable, code font JetBrains Mono Variable, heading weight 650 with `-0.02em` tracking, `--radius-base` 6px, `--radius-container` 12px. Light mode on surface-50, dark mode on surface-950, default from the system.
6. Mark. `src/lib/brand/Mark.svelte` draws the clipped disc: a solid circle with a 60° wedge cut from its upper right and that wedge drawn again shifted outward along its own bisector by 14% of the diameter. One fill in `currentColor`, a `size` prop, a `title` for assistive text. `src/lib/brand/Wordmark.svelte` places the mark in primary-500 beside "DiscoClip" in Inter 700 with `-0.03em` tracking.
7. Brand files. `static/favicon.svg` (mark in coral on transparent), `static/favicon.ico` (16, 32, 48), `static/icons/icon-192.png`, `static/icons/icon-512.png`, `static/icons/maskable-512.png`, `static/apple-touch-icon.png` (mark in coral on surface-950 with 20% padding), `static/site.webmanifest` (name DiscoClip, `display: standalone`, theme and background colors from the palette). `brand/` at the repository root holds the SVG sources and a 512 px PNG bot avatar exported from the same mark.
8. `src/lib/api/types.ts`: every request schema, response schema and enumeration in `API.md` as TypeScript types with the same names, plus the permission-by-role table as a constant.
9. `src/lib/api/client.ts`: `request<T>(method, path, body?, query?)` with JSON encode and decode, `credentials: 'same-origin'`, `x-csrf-token` from the session store on every mutating call, `ApiError` carrying the status, the message from `{ error }` and `Retry-After`, a `401` publishing an `unauthorized` event, `204` returning `undefined`. `fileUrl(path, query)` builds download and export links.
10. `src/lib/api/endpoints.ts`: one exported function per endpoint in `API.md`, grouped by section, typed with `types.ts`. Nothing calls `fetch` or `EventSource` outside `client.ts` and `events.svelte.ts`.
11. `src/lib/session.svelte.ts`: `WhoAmI` state, `refresh()` from `GET /api/session`, `login`, `logout`, `setup`, `recover`, `can(permission)` from the role table, and `goToLogin(next)` run by the client's `unauthorized` event.
12. `src/lib/events.svelte.ts`: one `EventSource` on `/api/events` per browser. Tabs compete for the `discoclip-events` Web Lock; the holder connects and rebroadcasts every event over a `BroadcastChannel` named `discoclip-events`; the other tabs consume from the channel and take over when the lock frees. Reconnect with exponential backoff from 1 s to 30 s, `state` of `connecting`, `live` or `offline`, `stats` as the latest `JobStats`, `bots` as a map of application id to `BotStatus` that drops entries on `removed`, and `onJob(handler)` subscriptions for `JobEvent`.
13. `src/lib/format.ts`: `bytes`, `clock` (h:mm:ss), `duration` from secs and nanos, `relative` time with an absolute `title`, `percent`, `number`, `discordIcon(guild, hash)` and `discordAvatar(user, hash)` CDN URLs, `mediaLabel`, with a Vitest test per function.
14. `src/lib/toast.svelte.ts` on Skeleton `Toast`: `notify.success`, `notify.error`, `notify.info`, and `reportError(err)` that turns an `ApiError` into a toast with its message and a countdown for `429`.
15. Shared components in `src/lib/components`: `PageHeader` (title, description, actions), `DataTable` (column defs, sort, sticky header, row select, empty state, placeholder rows while loading), `StatusBadge` for `JobStatus`, `BotState`, `HealthStatus`, `FixtureStatus` and `SessionState`, `Confirm` dialog, `CopyButton`, `RelativeTime`, `KeyValue`, `CodeBlock`, `EmptyState`, `ErrorState` with Retry, `Field` (label, help, error), `Bytes`, `Clock`, `MediaKindIcon`, `DiscordAvatar`, `GuildIcon`, `SearchInput` with debounce, `Pager` on Skeleton `Pagination`, `ModeToggle`.
16. Route groups: `(auth)` for `/setup`, `/login` and `/recover` with a centered card on a plain surface and the wordmark; `(app)` for the operator shell; `(front)` for `/f/[slug]` with its own shell and no operator session. `(app)/+layout.ts` calls `GET /api/setup` and redirects to `/setup` while `needed`, then `GET /api/session` and redirects to `/login?next=<path>` on `401`.
17. `(app)/+layout.svelte` shell: Skeleton `Navigation` as a rail at `md` and wider and a bottom bar below, entries Dashboard, Jobs, Applications, Servers, Profiles, Views, Platforms, Users, Settings, Audit, Health, Metrics, Logs, each shown only with its permission; `AppBar` with the wordmark, the page title, a feed state dot, a Submit button for `manage_jobs`, `ModeToggle`, and an account `Menu` (username, role, Account, Log out). The toaster mounts here.
18. `/setup`: username and password to `POST /api/setup`, then to `/`. Redirects to `/login` when `GET /api/setup` reports `needed` false.
19. `/login`: username and password to `POST /api/login`, one button per `GET /api/auth/providers` entry linking to `/api/auth/{id}/start?intent=login`, `next` honoured after login, `?error=` mapped from `CallbackError` to a sentence, a Recover link, `429` shown with its wait.
20. `/recover`: username, recovery key and new password to `POST /api/recover`, then to `/`.
21. `/` dashboard: stat tiles from `stats` (queued, running, done, failed, cancelled, last 24 h, workers active and waiting, queue depth), an active jobs list from `active` updated by `job` events with stage progress, a bots panel from `bots`, health from `GET /api/health` refreshed every 30 s, the ten newest jobs, and a Submit shortcut.
22. `/jobs`: `DataTable` over `GET /api/jobs` with `q`, `status`, `resolver`, `source`, `top_level`, `after`, `before` and `order` kept in the URL query, offset paging with `total`, rows updated in place from `job` events, row select with bulk Retry, Cancel and Delete through `POST /api/jobs/bulk` and a per-row outcome summary, columns thumbnail, title, media, status with stage, origin, destination, size, age.
23. `src/lib/components/SubmitDialog.svelte`: URL, limits (`max_source_bytes`, `max_duration_secs`, `max_height`), clip start and end, subtitle mode and language, `POST /api/jobs`, then navigate to `/jobs/[id]`. Opened from the app bar, the dashboard and `/jobs`.
24. `/jobs/[id]`: header with title, resolver, media kind and status; stage timeline from `timings` with live `progress`; request card (origin, url, destination, limits, options, submitted by, retry of, parent); resolved metadata and a variants table (kind, container, codecs, size, height, fps, bitrate, live, drm); source and output `MediaInfo`; published reference and link; archive entry; subtitles; children from `GET /api/jobs/{id}/children` for playlists; the log with live `log` events; a Download menu for output, source and each subtitle; Retry, Cancel and Delete with confirmation; an inline player for the output when the media is video, audio or image.
25. `/applications`: a card per `ApplicationView` with name, client id, bot state from `bots`, since, commands mode, login on or off; an Add dialog with bot token, name and client secret to `POST /api/discord/applications`; a Rules tab listing every watch rule from `GET /api/discord/rules` linking to its guild page.
26. `/applications/[id]`: bot panel (state, user, error, attempt, next attempt, Start, Stop, Restart); credentials panel (rename, replace bot token, set or remove client secret, login toggle, `login_callback_url` copy); commands panel (mode, guild picker, Save registers, Register again, registered at, last error, command list); install panel (`install_url` copy and open, scopes, permissions, optional guild); guilds table from `GET .../guilds` with icon, name, members, present or left, joined, linking to the guild page; Delete with confirmation.
27. Backend: `GET /api/discord/guilds/{guild}/applications` for `manage_watch_rules` or a session managing `guild`, returning `[{ application_id, name, present }]` for every application whose bot has been in the guild. Document it in `API.md` and cover it in `web/discord.rs` tests. This is what lets an account that manages a server on Discord reach its rules without `manage_applications`.
28. `/applications/[id]/guilds/[guild]`: application and guild names from item 27, channel tree from `GET .../channels` in Skeleton `TreeView` grouped by category in `position` order with `ChannelKind` icons and a rule badge, roles panel, Refresh, and the guild's rules from `GET .../rules` (channel, post to, users, roles, enabled, updated). Loads for anyone the backend admits.
29. Rule dialog: `post_to` channel select, `allow_users` picker searching `GET .../members?q=` in a `Combobox` with avatars and resolving stored ids through `GET .../members/{user}`, `allow_roles` picker with role colors, `enabled`, create through `POST .../rules`, edit through `PUT /api/discord/rules/{id}`, delete through `DELETE`.
30. Profile assignments on the guild page: guild, channel and member assignments from `GET /api/profiles/assignments?guild=`, assign or clear through `PUT` and `DELETE /api/profiles/assignments/{scope}`, and an effective preview from `GET /api/profiles/effective?guild&channel&user` showing limits, the applied chain widest first and the disabled platforms.
31. `/servers`: the account's guilds from `GET /api/discord/guilds` with icon, name, owner and manageable, Refresh through `POST .../refresh`, the applications present in each manageable guild from item 27 linking to the guild page. Shown in the navigation only for accounts with a Discord identity.
32. `/profiles`: table of profiles with a builtin badge and the global default marked from the global assignment, Set as default through `PUT /api/profiles/assignments/global`, New, Delete with `409` explained.
33. `/profiles/[id]` and `/profiles/new`: name, description, limits, platform default (inherit, enabled, disabled), preset chips from `GET /api/profiles/presets`, a per-platform override grid from `GET /api/platforms` showing the computed result per platform, Save through `POST` or `PUT /api/profiles`.
34. `/views`: table of content views (name, slug, enabled, access summary, downloads, links, profile), share link copy for `/f/<slug>`, New, Delete.
35. `/views/[id]` and `/views/new`: general (name, slug, description, enabled, profile, downloads), scope (guild and channel pickers across every application's guilds), access (open, secret kind, accounts, providers from `GET /api/auth/providers`, discord members, discord users in a `TagsInput`), links policy (enabled, min height, min bitrate, max bytes, signed link days) with the `web.public_url` requirement shown, Save through `POST` or `PUT /api/frontends`.
36. `/views/[id]` panels: shared secret set or remove through `PUT .../secret`; view accounts list, create, set password and delete through `.../users`; viewer sessions with subject, display, ip, agent and last seen, revoke one and revoke all.
37. `/f/[slug]`: `GET /api/f/{slug}` header with name and description, media grid from `GET /api/f/{slug}/jobs` with thumbnail, title, uploader, duration, media kind and live badge, filters `q`, `media` and `resolver`, infinite scroll on `next`, card click opening the media page, Download when `downloads`, viewer display and Log out, redirect to `/f/[slug]/login` on `401`.
38. `/f/[slug]/login`: secret input as PIN, password or token per `access.secret`, account form when `access.accounts`, one button per provider to `/api/f/{slug}/auth/{id}/start`, `?error=` reasons (`state`, `denied`, `provider`, `exchange`, `identity`, `frontend`, `not_listed`, `not_member`, `guilds`) as sentences.
39. `/f/[slug]/j/[id]`: player for video and audio, image view, file card, title, uploader, duration, size, dimensions, published time, source link, Download when allowed, back to the gallery. The server supplies the head metadata; the route renders the body.
40. `/platforms`: grid of `PlatformCoverage` with name, hosts, media kinds, tags, session support, fixture pass and fail counts, last run, running spinner and cookies count; filters by tag, media, session and text; Check all through `POST /api/platforms/check` for `manage_jobs`.
41. `/platforms/[id]`: hosts, features, formats, media, tags; fixtures table (url, status, found, duration, run at, error); Check through `POST /api/platforms/{id}/check`; session panel with cookies count, updated at, last session check state and account.
42. Cookies dialog on `/platforms/[id]`: Netscape file through `FileUpload` or paste, Cookie header with domain, `PUT /api/platforms/{id}/cookies` with the `SessionOutcome` shown, Clear cookies through `DELETE`, Check session through `POST .../session/check`.
43. `/settings`: search box filtering keys; sections from the `defaults` tree (`data_dir`, `log`, `engine`, `http`, `local`, `fixtures`, `web`, `auth`) rendered as fields typed from the default value (string, number, boolean, list, map, path), each with current value, default, `source` badge and updated at, Edit through `PUT /api/settings/{key}`, Reset through `DELETE`, keys in `secrets` masked with a Replace action, `data_dir` and `provisioning_file` in the header. Login providers github, google and oidc are edited here.
44. Settings Import dialog (`format`, text or file) to `POST /api/settings/import` and an Export menu linking `GET /api/settings/export?format=` for toml, yaml and json.
45. `/users`: accounts table (username, role, password set, created), Create dialog (username, role, optional password), role change through `PATCH`, Set password, Delete with confirmation; roles panel from `GET /api/roles` listing permissions and members.
46. `/users` Sessions and Tokens tabs: `GET /api/sessions/all` with revoke one and revoke all for an account through the `/api/users/{id}/sessions` routes; `GET /api/tokens/all` with revoke through `DELETE /api/users/{id}/tokens/{token}`.
47. `/account`: change password with current password, sessions with the current one marked, End others and End one; identities from `GET /api/auth/identities` with a Link button per provider to `/api/auth/{id}/start?intent=link`, Unlink, Refresh, `?error=` from `CallbackError`.
48. `/account` API tokens: list with prefix, scopes, last used and expiry; Create dialog with name, scope checklist and expiry days, then a secret dialog shown once with copy; Revoke.
49. `/audit`: filters actor, action, target kind and id, since and until in `DatePicker`; cursor paging on `before`; rows with time, actor (user with via and ip, or provisioning file), action and target; expand to show `details` with `value` and `previous` side by side.
50. `/health`: overall status, version, started, uptime, checks with label, status and detail, refresh every 15 s.
51. `/metrics`: stat tiles for process (rss, cpu, run time), system (memory, load, cpus), jobs, bots by state, cache, database, fixtures and log buffer; a disk meter per mount with `holds`; requests by host and status table; refresh every 5 s.
52. `/logs`: level, target and text filters; list from `GET /api/logs` with load older on `next`; live tail from `GET /api/logs/events` with Pause and Resume and a `skipped` banner; level colors; fields expanded per line; `buffered` of `capacity` shown.
53. `src/routes/+error.svelte` for 404 and load failures with the wordmark and a way back; `(front)` gets its own error page without operator links.
54. Loading, empty and error states: every list shows placeholder rows while loading, `EmptyState` with an action when empty, and `ErrorState` with Retry on failure; every mutating button disables while pending.
55. Responsive pass at 360, 768 and 1280 px: tables scroll inside their container, dialogs go full screen below `sm`, the rail becomes the bottom bar, the gallery grid steps from 1 to 4 columns.
56. Accessibility pass: visible focus rings, a label on every control, `aria-live` for toasts and feed state, dialogs trap focus, `prefers-reduced-motion` disables transitions, contrast of every badge and status color checked in both modes.
57. `crates/app/ui/DESIGN.md`: the design system (palette, type, spacing, radius, the components in use) and the writing standards for labels, buttons, empty states, errors and confirmations.
58. Vitest: `client.ts` (csrf header, error parsing, `401` event, `429` retry), `events.svelte.ts` (leader election and takeover with mocked locks and channel), `format.ts`, the permission table.
59. Playwright against a backend started with a temporary `data_dir`: setup, login, role gating of the navigation, submit dialog validation, settings edit and reset, view creation and gallery login, account token creation.
60. README: update the Build and Run sections for the new `ui` scripts, replace the hero screenshot with a dashboard capture in both modes, link `DESIGN.md`.
61. Acceptance: `npm run check`, `npm run lint`, `npm test`, `npm run build`, `cargo build --release` embedding the app, `cargo test --workspace`, and a walk through every route as admin, operator and viewer.
